package com.dahua.messaging.enumeration;

//Gender = Either Male or Female

//Enum: is a special class used to define a fixed set of constants.
// It helps represent a group of related values that don't change,
// like days of the week, directions, or statuses.
public enum Gender {
    MALE,
    FEMALE;

}
